import { LimitCharecterPipe } from './limit-charecter.pipe';

describe('LimitCharecterPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitCharecterPipe();
    expect(pipe).toBeTruthy();
  });
});
